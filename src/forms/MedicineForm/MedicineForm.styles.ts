import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	wrapper: {
		flex: 1,
		gap: 16,
	},

	title: {
		fontSize: 20,
	},

	fieldsWrapper: {
		flex: 1,
		gap: 16,
	},

	input: {
		fontSize: 16,
	},

	actions: {
		gap: 8,
	},
})
