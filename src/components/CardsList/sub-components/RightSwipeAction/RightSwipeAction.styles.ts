import { StyleSheet } from 'react-native'

import { CARD_HEIGHT } from '@app/constants'

export const styles = StyleSheet.create({
	wrapper: {
		height: CARD_HEIGHT,
		borderRadius: 16,
		flexDirection: 'row-reverse',
	},
})
