import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	infoWrapper: {
		flex: 1,
		justifyContent: 'center',
	},

	info: {
		gap: 16,
		alignItems: 'center',
		alignSelf: 'center',
	},

	buttonWrapper: {
		gap: 8,
		padding: 16,
		borderRadius: 24,
		flexDirection: 'row',
		alignItems: 'center',
		justifyContent: 'center',
	},
})
